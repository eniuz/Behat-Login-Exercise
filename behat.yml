default:
    extensions:
        Behat\Symfony2Extension\Extension:
        mink_driver: true
        kernel:
            env: test
            debug: true
        Behat\MinkExtension\Extension:
            goutte: ~
            selenium2: ~
            base_url: 'http://login.workshop.local/app_dev.php/'
            show_cmd: google-chrome %s
            javascript_session:  'selenium2'
